// This event for server listen and handle
export enum SUBSCRIBE_EVENT {
  INIT = 'init',
  TIME = 'time',
}

// This event for client listen and handle
export enum PUBLISH_EVENT {
  INIT = 'init',
}
